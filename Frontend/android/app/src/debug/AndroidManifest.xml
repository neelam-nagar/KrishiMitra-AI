<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow cleartext traffic in debug builds only (for emulator testing) -->
    <application
        android:usesCleartextTraffic="true">
    </application>
</manifest>
